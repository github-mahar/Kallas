<div class="hdt-article-content hdt-rte" timeline hdt-reveal="slide-in">
  <article id="hdt-article-{{ article.id }}" class="article-{{ article.id }}">{{ article.content }}</article>
</div> 
{% schema %}
  {
    "name": "t:content",
    "tag": null,
    "settings": [
    
    ],
    "presets":[
      {
        "name": "t:content"
      }
    ]
  }
{% endschema %}
